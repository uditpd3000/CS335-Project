// class MyClass extends MySuperClass implements YourInterface {
    // int x, y;
    // int x = 3;
    
    // private Point() {}
    // final int baby(int ab.this,){}
    // class rosey permits Udi{}
    // Point(this,) { this(x instanceof int[]); }
    // Point(int x) {z = a++;
      int[][].class.aisehi = 3;
      z=6;
      super.udit.name="ab";
      udit.name = (int)"mine";
      
      a[4] = 5;
      this.name = "udit";
      void.class.name = you;
      super.name = kyaabat;
      udit.this.wow=3;
      udit.wiw.kdkkkd.super.mneeee= "rosey";
      a &= d*f+r;
      a = k%z||x;
      z= b++;
      k = ++c ;
      r = c== ros;
      kyoo= x&&y;
      x instanceof y;
      udit(mai = mast, you = also_mast);

    // }
    // Test() {}
      // No receiver parameter is permitted in the constructor of
      // a top level class, as there is no conceivable type or name. 
// }